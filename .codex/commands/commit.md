From the currently staged changes (git diff --staged), generate a Conventional Commit message.

Requirements:

- Use format: type(scope): message
- Include a short summary (max 1 line)
- Add bullet points describing key changes
- Be concise but clear
- Do NOT perform commit

Return only the commit message.
